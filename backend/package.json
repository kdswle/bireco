{
  "name": "bireco-backend",
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build --release",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}